const recipeImageInfoContainer = document.getElementById("image-container");

function printRecipeCards(recipeArray) {
  recipeContainer.innerHTML = "";
  // console.log(recipeArray);

  recipeArray.forEach((recipe) => {});
}
